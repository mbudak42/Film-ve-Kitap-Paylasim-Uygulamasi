<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Giriş Yap - Sosyal Kütüphane</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #261E1C 0%, #733B27 100%);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
        }

        .container {
            background: #F0F2F2;
            border-radius: 20px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
            overflow: hidden;
            max-width: 900px;
            width: 100%;
            display: flex;
            min-height: 500px;
        }

        .left-panel {
            background: linear-gradient(135deg, #8C583A 0%, #A66D58 100%);
            padding: 60px 40px;
            flex: 1;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            color: #F0F2F2;
            text-align: center;
        }

        .left-panel h1 {
            font-size: 2.5em;
            margin-bottom: 20px;
            font-weight: 600;
        }

        .left-panel p {
            font-size: 1.1em;
            line-height: 1.6;
            opacity: 0.95;
        }

        .right-panel {
            flex: 1;
            padding: 60px 50px;
            display: flex;
            flex-direction: column;
            justify-content: center;
        }

        .form-title {
            color: #261E1C;
            font-size: 2em;
            margin-bottom: 10px;
            font-weight: 600;
        }

        .form-subtitle {
            color: #733B27;
            margin-bottom: 30px;
            font-size: 0.95em;
        }

        .form-group {
            margin-bottom: 25px;
        }

        label {
            display: block;
            margin-bottom: 8px;
            color: #261E1C;
            font-weight: 500;
            font-size: 0.9em;
        }

        input {
            width: 100%;
            padding: 14px 18px;
            border: 2px solid #A66D58;
            border-radius: 10px;
            font-size: 1em;
            transition: all 0.3s;
            background: #F0F2F2;
            color: #261E1C;
        }

        input:focus {
            outline: none;
            border-color: #8C583A;
            box-shadow: 0 0 0 3px rgba(140, 88, 58, 0.1);
        }

        .forgot-password {
            text-align: right;
            margin-top: -15px;
            margin-bottom: 25px;
        }

        .forgot-password a {
            color: #8C583A;
            text-decoration: none;
            font-size: 0.9em;
            transition: color 0.3s;
        }

        .forgot-password a:hover {
            color: #733B27;
        }

        .btn {
            width: 100%;
            padding: 15px;
            background: linear-gradient(135deg, #8C583A 0%, #733B27 100%);
            color: #F0F2F2;
            border: none;
            border-radius: 10px;
            font-size: 1.1em;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s;
            box-shadow: 0 4px 15px rgba(140, 88, 58, 0.3);
        }

        .btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(140, 88, 58, 0.4);
        }

        .btn:active {
            transform: translateY(0);
        }

        .register-link {
            text-align: center;
            margin-top: 25px;
            color: #733B27;
            font-size: 0.95em;
        }

        .register-link a {
            color: #8C583A;
            text-decoration: none;
            font-weight: 600;
            transition: color 0.3s;
        }

        .register-link a:hover {
            color: #733B27;
        }

        .error-message {
            background: #ffebee;
            color: #c62828;
            padding: 12px 18px;
            border-radius: 8px;
            margin-bottom: 20px;
            border-left: 4px solid #c62828;
            font-size: 0.9em;
            display: none;
        }

        .error-message.show {
            display: block;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="left-panel">
            <h1>📚 Sosyal Kütüphane</h1>
            <p>Kitaplarınızı ve filmlerinizi keşfedin, puanlayın ve paylaşın. Okuma ve izleme deneyiminizi sosyal hale getirin.</p>
        </div>
        <div class="right-panel">
            <h2 class="form-title">Hoş Geldiniz</h2>
            <p class="form-subtitle">Hesabınıza giriş yapın</p>
            
            <div class="error-message" id="errorMessage">
                E-posta veya şifre hatalı. Lütfen tekrar deneyin.
            </div>

            <form id="loginForm">
                <div class="form-group">
                    <label for="email">E-posta</label>
                    <input type="email" id="email" name="email" placeholder="ornek@email.com" required>
                </div>
                <div class="form-group">
                    <label for="password">Şifre</label>
                    <input type="password" id="password" name="password" placeholder="••••••••" required>
                </div>
                <div class="forgot-password">
                    <a href="/sifremi-unuttum">Şifremi Unuttum</a>
                </div>
                <button type="submit" class="btn">Giriş Yap</button>
            </form>
            
            <div class="register-link">
                Hesabınız yok mu? <a href="/kayit-ol">Kayıt Olun</a>
            </div>
        </div>
    </div>

    <script>
        document.getElementById('loginForm').addEventListener('submit', function(e) {
            e.preventDefault();
            
            // Form verilerini al
            const formData = {
                email: document.getElementById('email').value,
                password: document.getElementById('password').value
            };

            // Burada .NET API'nize POST isteği yapacaksınız
            // Örnek:
            /*
            fetch('/api/auth/login', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                },
                body: JSON.stringify(formData)
            })
            .then(response => response.json())
            .then(data => {
                if (data.success) {
                    window.location.href = '/ana-sayfa';
                } else {
                    document.getElementById('errorMessage').classList.add('show');
                }
            })
            .catch(error => {
                console.error('Error:', error);
                document.getElementById('errorMessage').classList.add('show');
            });
            */
            
            console.log('Login data:', formData);
        });
    </script>
</body>
</html>