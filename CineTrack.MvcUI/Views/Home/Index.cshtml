@model List<dynamic>
@{
    ViewData["Title"] = "Ana Sayfa";
}

<div class="text-center mb-5">
    <h1 class="display-4">🎬 CineTrack'e Hoş Geldiniz</h1>
    <p class="lead">Film ve kitaplarınızı takip edin, paylaşın!</p>
</div>

@if (Context.Session.GetString("token") == null)
{
    <div class="alert alert-info text-center">
        <strong>Merhaba!</strong> Devam etmek için lütfen <a asp-controller="Auth" asp-action="Login" class="alert-link">giriş yapın</a> 
        veya <a asp-controller="Auth" asp-action="Register" class="alert-link">kayıt olun</a>.
    </div>
}
else
{
    <h3>Takip Ettiklerinizin Aktiviteleri</h3>
    
    @if (Model == null || !Model.Any())
    {
        <div class="alert alert-secondary">
            Henüz aktivite yok. Başlamak için kullanıcıları takip edin!
        </div>
    }
    else
    {
        <div class="row">
            @foreach (var item in Model)
            {
                <div class="col-md-6 mb-3">
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title">@item.username</h5>
                            <p class="card-text">
                                <strong>@item.actionType</strong> yaptı<br />
                                <em>@item.targetTitle</em>
                            </p>
                            <small class="text-muted">@item.createdAt</small>
                        </div>
                    </div>
                </div>
            }
        </div>
    }
}